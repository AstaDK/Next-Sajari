import { Analytics } from './Analytics';
export declare class DebugAnalytics {
    constructor(analytics: Analytics);
    pageClosed: (bodyToSend: string) => void;
    bodyReset: (bodyToSend: string) => void;
    resultClicked: (bodyToSend: string) => void;
}
