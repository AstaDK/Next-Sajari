import { Session } from '@sajari/sdk-js';
import { CallbackFn, UnlistenFn } from '../Listener';
export declare type TrackingValues = Record<string, string>;
export declare class Tracking {
    clientTracking: Session | null;
    private listeners;
    field: string;
    constructor();
    listen(event: string, callback: CallbackFn): UnlistenFn;
    private emitTrackingReset;
    reset(variables?: TrackingValues): void;
    next(variables: TrackingValues): import("@sajari/sdk-js").Tracking;
}
