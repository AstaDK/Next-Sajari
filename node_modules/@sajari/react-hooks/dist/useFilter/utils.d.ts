import { SortType } from './types';
export declare type Item = [string, number | string | (() => string)];
export declare const sortItems: (list: Item[], sort?: SortType, asc?: boolean) => Item[];
