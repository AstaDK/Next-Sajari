import { FilterItem, SortType } from './types';
declare function useFilter(name: string, params?: {
    sort?: SortType;
    sortAscending?: boolean;
}): {
    options: FilterItem[];
    setSelected: (value: string[], merge?: boolean) => void;
    selected: string[];
    reset: () => void;
    multi: boolean;
};
export default useFilter;
export * from './types';
