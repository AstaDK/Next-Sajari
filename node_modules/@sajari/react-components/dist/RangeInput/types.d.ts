import { AriaTextFieldOptions, TextFieldAria } from '@react-aria/textfield';
import { Range } from '@sajari/react-hooks';
import * as React from 'react';
import { BoxProps } from '../Box';
export interface CustomInputProps {
    getProps: (override?: AriaTextFieldOptions) => TextFieldAria & {
        ref: React.MutableRefObject<HTMLInputElement | null>;
    };
}
export declare type RangeValue = number | Range;
export declare type RangeInputValue = [string, string?];
export interface RangeInputProps<T> extends BoxProps {
    language?: string;
    format?: 'default' | 'price';
    currency?: string;
    fixedPoint?: boolean;
    min?: number;
    max?: number;
    step?: number;
    steps?: number[];
    tick?: number;
    ticks?: number[];
    value?: T;
    onChange?: (value: T) => void;
    onInput?: (value: T) => void;
    leftInput?: (props: CustomInputProps) => React.ReactNode;
    rightInput?: (props: CustomInputProps) => React.ReactNode;
    showInputs?: boolean;
    trackClassName?: string;
    fillClassName?: string;
    handleClassName?: string;
    handleActiveClassName?: string;
    inputClassName?: string;
}
