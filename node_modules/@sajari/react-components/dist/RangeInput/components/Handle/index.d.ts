import * as React from 'react';
import { HandleProps } from './types';
declare const Handle: React.ForwardRefExoticComponent<HandleProps & React.RefAttributes<HTMLButtonElement>>;
export default Handle;
