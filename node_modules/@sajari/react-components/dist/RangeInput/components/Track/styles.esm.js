import { mapStyles } from '@sajari/react-sdk-utils';

function useTrackStyles() {
  var styles = {
    container: [{
      "position": "relative",
      "flex": "1 1 0%",
      "height": "0.25rem"
    }, 'touch-action: none;']
  };
  return mapStyles(styles);
}

export default useTrackStyles;
//# sourceMappingURL=styles.esm.js.map
