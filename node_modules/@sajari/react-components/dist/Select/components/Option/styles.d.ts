import { SelectProps } from '../../types';
import { OptionProps } from './types';
interface UseOptionStylesParams extends Omit<OptionProps, 'size'> {
    highlighted: boolean;
    selected: boolean;
    size: SelectProps['size'];
}
export declare function useOptionStyles(params: UseOptionStylesParams): {
    styles: Record<"children" | "label" | "option", import("@emotion/utils").SerializedStyles>;
};
export {};
