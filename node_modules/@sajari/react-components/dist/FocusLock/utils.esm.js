var focusableElements = ['a[href]', 'area[href]', 'button:not([disabled])', 'embed', 'iframe', 'input:not([disabled])', 'object', 'select:not([disabled])', 'textarea:not([disabled])', '*[tabindex]:not([aria-disabled])', '*[contenteditable]'];
var focusableElSelector = /*#__PURE__*/focusableElements.join();
function getFocusables(element, keyboardOnly) {
  if (keyboardOnly === void 0) {
    keyboardOnly = false;
  }

  if (element === null) {
    return [];
  }

  var elements = Array.from(element.querySelectorAll(focusableElSelector));
  elements = elements.filter(function (focusableEl) {
    return window.getComputedStyle(focusableEl).display !== 'none';
  });

  if (keyboardOnly === true) {
    elements = elements.filter(function (focusableEl) {
      return focusableEl.getAttribute('tabindex') !== '-1';
    });
  }

  return elements;
}

export { getFocusables };
//# sourceMappingURL=utils.esm.js.map
