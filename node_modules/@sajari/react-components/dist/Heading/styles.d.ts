import { FontSizes } from '../hooks';
import { HeadingElements } from './types';
interface UseHeadingStylesOptions {
    as: HeadingElements;
    size?: FontSizes;
    truncate?: boolean | 2 | 3 | 4 | 5;
}
export default function useHeadingStyles(options: UseHeadingStylesOptions): Record<"container", import("@emotion/utils").SerializedStyles>;
export {};
