import { TextElement, TextProps } from './types';
interface UseTextStylesProps {
    as: TextElement;
    truncate: TextProps['truncate'];
    size: TextProps['size'];
}
export default function useTextStyles({ as, truncate, size }: UseTextStylesProps): Record<"container", import("@emotion/utils").SerializedStyles>;
export {};
