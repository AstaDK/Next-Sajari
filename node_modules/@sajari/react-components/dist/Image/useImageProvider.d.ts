import { ImageProvider } from './types';
interface Params {
    provider: ImageProvider;
    images?: string | Array<string | undefined>;
    width?: number | string;
    height?: number | string;
}
export default function useImageProvider(params: Params): string[];
export {};
