import { useTheme } from '@sajari/react-sdk-utils';
import ColorClass from 'color';

function useRingStyles(props) {
  var theme = useTheme();

  var _ref = props != null ? props : {},
      _ref$visible = _ref.visible,
      visible = _ref$visible === void 0 ? true : _ref$visible,
      disabled = _ref.disabled,
      invalid = _ref.invalid,
      _ref$rounded = _ref.rounded,
      rounded = _ref$rounded === void 0 ? 'lg' : _ref$rounded,
      _ref$color = _ref.color,
      color = _ref$color === void 0 ? theme.color.primary.base : _ref$color;

  var styles = [];

  if (visible) {
    var borderColor = color;

    if (disabled) {
      borderColor = "#6b7280".toString();
    } else if (invalid) {
      borderColor = "#f05252".toString();
    }

    var borderRadius;

    switch (rounded) {
      case 'sm':
        borderRadius = {
          ":after": {
            "borderRadius": "0.375rem"
          }
        };
        break;

      case 'md':
        borderRadius = {
          ":after": {
            "borderRadius": "0.375rem"
          }
        };
        break;

      case 'full':
        borderRadius = {
          ":after": {
            "borderRadius": "9999px"
          }
        };
        break;

      case 'none':
        borderRadius = {
          ":after": {
            "borderRadius": "0"
          }
        };
        break;

      case 'lg':
      default:
        borderRadius = {
          ":after": {
            "borderRadius": "0.5rem"
          }
        };
    }

    styles.push({
      ":after": {
        "content": "\"\"",
        "boxSizing": "content-box",
        "display": "block",
        "height": "100%",
        "width": "100%",
        "borderWidth": "2px",
        "borderStyle": "solid",
        "padding": "0.125rem",
        "position": "absolute"
      }
    }, "&::after {\n          left: -0.25rem;\n          top: -0.25rem;\n          border-color: " + new ColorClass(borderColor) + ";\n        }", borderRadius, {
      "outline": "2px solid transparent",
      "outlineOffset": "2px"
    });
  }

  return styles;
}

export default useRingStyles;
//# sourceMappingURL=useRingStyles.esm.js.map
