import { mapStyles, useTheme, inferStylesObjectKeys } from '@sajari/react-sdk-utils';

function useDropdownItemStyles(params) {
  var highlighted = params.highlighted;
  var theme = useTheme();
  var styles = inferStylesObjectKeys({
    root: [{
      "display": "flex",
      "alignItems": "center",
      "width": "auto",
      "paddingLeft": "0.5rem",
      "paddingRight": "0.5rem",
      "paddingTop": "0.25rem",
      "paddingBottom": "0.25rem",
      "lineHeight": "1.25rem",
      "textAlign": "left",
      "transitionProperty": "all",
      "transitionDuration": "75ms",
      "borderRadius": "0.25rem",
      "cursor": "pointer"
    }],
    label: [{
      "paddingLeft": "1rem",
      "marginLeft": "auto",
      "fontSize": "0.75rem",
      "--text-opacity": "1",
      "color": "rgba(159, 166, 178, var(--text-opacity))"
    }]
  });

  if (highlighted) {
    styles.root.push("color: " + theme.color.primary.text + "; background: " + theme.color.primary.active + ";");
    styles.label.push("color: " + theme.color.primary.text + ";");
  } else {
    styles.root.push({
      "--text-opacity": "1",
      "color": "rgba(75, 85, 99, var(--text-opacity))"
    });
  }

  return mapStyles(styles);
}

export { useDropdownItemStyles };
//# sourceMappingURL=useDropdownItemStyles.esm.js.map
