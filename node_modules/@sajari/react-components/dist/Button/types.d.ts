import { AriaButtonProps } from '@react-types/button';
import { PropsWithAs } from '@sajari/react-sdk-utils';
import { BoxProps } from '../Box';
export declare type ButtonAppearance = 'default' | 'primary' | 'secondary' | 'link' | 'subtle-link' | 'none';
export declare type ButtonSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
export declare type ButtonSpacing = 'compact' | 'default' | 'none';
interface Props extends BoxProps {
    appearance?: ButtonAppearance;
    spacing?: ButtonSpacing;
    size?: ButtonSize;
    disabled?: boolean;
    loading?: boolean;
    fullWidth?: boolean;
    pressedClassName?: string;
}
export interface ButtonProps extends Omit<PropsWithAs<Props>, keyof AriaButtonProps>, AriaButtonProps {
}
export {};
