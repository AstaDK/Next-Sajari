import { PropsWithAs } from '@sajari/react-sdk-utils';
import * as React from 'react';
import { BoxProps } from '../Box';
export interface Props extends BoxProps {
    attached?: boolean;
    fullWidth?: boolean;
    inline?: boolean;
    children: React.ReactNode;
}
export declare type UseButtonGroupStylesParams = Pick<Props, 'attached' | 'inline'>;
export declare type ButtonGroupProps = PropsWithAs<Props>;
