import * as React from 'react';
declare type CreateContextReturn<T> = [React.Provider<T>, () => T, React.Context<T>];
interface CreateContextOptions {
    strict?: boolean;
    errorMessage?: string;
    name?: string;
}
export default function createContext<ContextType>(options?: CreateContextOptions): CreateContextReturn<ContextType>;
export {};
