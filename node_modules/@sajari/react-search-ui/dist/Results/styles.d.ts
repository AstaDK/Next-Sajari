import { ResultsProps } from './types';
interface Props extends ResultsProps {
    width: number;
}
export default function useResultsStyles({ appearance, columnMinWidth, columns: columnsProp, gap: gapProp, width, }: Props): Record<"container", import("@emotion/utils").SerializedStyles>;
export {};
