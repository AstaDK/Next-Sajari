import { FilterItem } from '@sajari/react-hooks';
import { TFunction } from 'i18next';
export declare function pinItems(list: FilterItem[], pinned: string[] | undefined, prop: string): FilterItem[];
export declare function getHeaderId(name: string): string;
interface FormatValueParams {
    format?: 'default' | 'price';
    currency?: string;
    t: TFunction;
}
export declare function formatLabel(input: string, params: FormatValueParams): string;
export {};
