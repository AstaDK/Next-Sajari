import 'react';
import { jsx } from '@emotion/core/jsx-runtime';
import { objectWithoutPropertiesLoose as _objectWithoutPropertiesLoose, extends as _extends } from '../_virtual/_rollupPluginBabelHelpers.js';
import ColorFilter from './ColorFilter.esm.js';
import ListFilter from './ListFilter.esm.js';
import RangeFilter from './RangeFilter.esm.js';
import RatingFilter from './RatingFilter.esm.js';
import SelectFilter from './SelectFilter.esm.js';
import TabFilter from './TabFilter.esm.js';

var Filter = function Filter(_ref) {
  var _ref$type = _ref.type,
      type = _ref$type === void 0 ? 'list' : _ref$type,
      rest = _objectWithoutPropertiesLoose(_ref, ["type"]);

  if (type === 'color') {
    return jsx(ColorFilter, _extends({}, rest));
  }

  if (type === 'range') {
    return jsx(RangeFilter, _extends({}, rest));
  }

  if (type === 'rating') {
    return jsx(RatingFilter, _extends({}, rest));
  }

  if (type === 'select') {
    return jsx(SelectFilter, _extends({}, rest));
  }

  if (type === 'tabs') {
    return jsx(TabFilter, _extends({}, rest));
  }

  return jsx(ListFilter, _extends({}, rest));
};

export default Filter;
//# sourceMappingURL=index.esm.js.map
