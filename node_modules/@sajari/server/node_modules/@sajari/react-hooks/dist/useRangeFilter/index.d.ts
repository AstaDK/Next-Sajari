import { Range } from '../ContextProvider/controllers/filters/types';
declare function useRangeFilter(name: string): {
    min: number;
    max: number;
    step: number;
    setRange: (value: Range) => void;
    range: Range | null;
    reset: () => void;
    showReset: boolean;
};
export default useRangeFilter;
